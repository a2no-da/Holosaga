using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

[System.Serializable]
public class CharacterEndPanel
{
    public GameObject panelObject;
}

public class Finish : MonoBehaviour
{
 /*   public List<CharacterEndPanel> characterEndPanels = new List<CharacterEndPanel>();
    public List<Tower> allTowers = new List<Tower>();

    void Start()
    {
        int count = GameManager.Instance.unitCount;
        switch (count)
        {
            case 4:
                break;
            case 3:
                characterEndPanels[3].panelObject.SetActive(false);
                break;
            case 2:
                characterEndPanels[2].panelObject.SetActive(false);
                characterEndPanels[3].panelObject.SetActive(false);
                break;
            case 1:
                characterEndPanels[3].panelObject.SetActive(false);
                characterEndPanels[2].panelObject.SetActive(false);
                characterEndPanels[1].panelObject.SetActive(false);
                break;
        }
        allTowers = GameManager.instance.AllTowers;
    }

    // Update is called once per frame
    void Update()
    {

    }*/
}
